[project]
name = "360-report-automation"
version = "0.1.0"
description = "360-degree report generation workflow (deep research + section extraction)"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "pg8000>=1.30.0",
    "python-dotenv>=1.0.0",
    "pandas>=2.0.0",
    "openpyxl>=3.1.0",
]

[tool.uv]
package = false
